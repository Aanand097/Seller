# TanStack Start is deployed on Netlify with the Nitro Netlify server preset.
# Do not add an SPA catch-all here: the generated Netlify server redirect must
# handle deep links, refreshes, server rendering, and server functions.